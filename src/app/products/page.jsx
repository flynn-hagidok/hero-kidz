import Products from "@/components/home/Products";

export const metadata = {
    title: "Products",
}

const Product = () => {
    return (
        <div>
            <Products></Products>
        </div>
    )
};

export default Product;